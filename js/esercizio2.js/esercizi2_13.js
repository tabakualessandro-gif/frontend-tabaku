const raggio = 5;
const PI = Math.PI;

const area = PI * Math.pow(raggio, 2);
const circonferenza = 2 * PI * raggio;

console.log(`Area: ${area.toFixed(2)}, Circonferenza: ${circonferenza.toFixed(2)}`);